# Backend
PORT=8080
DATABASE_URL=postgresql://user:password@localhost/phone_guard

# LINE Notify
LINE_NOTIFY_TOKEN=your_line_notify_token_here

# Backend URL (for ESP32 to report calls to cloud)
BACKEND_URL=http://localhost:8080
